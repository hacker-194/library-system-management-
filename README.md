# library-system-management-
A SQL-based Library Management System built using PostgreSQL to manage books, members, employees, issue records, and return records efficiently using relational database concepts.
